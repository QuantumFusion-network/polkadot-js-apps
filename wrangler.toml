name = "portal-worker"
main = "worker/index.ts"
compatibility_date = "2024-10-01"
workers_dev = true

assets = { directory = "packages/apps/build", binding = "ASSETS" }

# включим позже для домена:
# routes = [{ pattern = "portal.qfnetwork.xyz/*", zone_name = "qfnetwork.xyz" }]
